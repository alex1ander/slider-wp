<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{ head }}
</head>
<body>
<header>
    <nav>
        {% for i in range(0, menu - 1) %}
        <a class="slider-button" href="#swiper{{ i  + 1 }}">Slider {{ i  + 1 }}</a>
        {% endfor %}
    </nav>
</header>


